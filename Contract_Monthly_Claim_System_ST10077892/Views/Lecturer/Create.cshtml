@model Contract_Monthly_Claim_System_ST10077892.Models.Lecturer

@{
    ViewData["Title"] = "Create Lecturer";
}

<div class="container mt-5">
    <div class="card shadow-lg rounded-4 animate__animated animate__fadeInUp">
        <div class="card-body p-5">
            <h1 class="mb-4 text-center text-primary fw-bold animate__animated animate__fadeInDown">Create New Lecturer 👩‍🏫</h1>
            <h4 class="mb-3 text-center text-secondary">Enter lecturer details below</h4>
            <hr />

            <div class="row justify-content-center">
                <div class="col-md-6">
                    <form asp-action="Create" class="needs-validation" novalidate>
                        @Html.AntiForgeryToken()
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="mb-3">
                            <label asp-for="LecturerID" class="form-label fw-semibold"></label>
                            <input asp-for="LecturerID" class="form-control shadow-sm" placeholder="Enter Lecturer ID" />
                            <span asp-validation-for="LecturerID" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="FullName" class="form-label fw-semibold"></label>
                            <input asp-for="FullName" class="form-control shadow-sm" placeholder="Enter Full Name" />
                            <span asp-validation-for="FullName" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Email" class="form-label fw-semibold"></label>
                            <input asp-for="Email" type="email" class="form-control shadow-sm" placeholder="Enter Email" />
                            <span asp-validation-for="Email" class="text-danger"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="Department" class="form-label fw-semibold"></label>
                            <input asp-for="Department" class="form-control shadow-sm" placeholder="Enter Department" />
                            <span asp-validation-for="Department" class="text-danger"></span>
                        </div>

                        <div class="d-grid">
                            <input type="submit" value="Create Lecturer" class="btn btn-primary btn-lg fw-bold shadow-sm" />
                        </div>
                    </form>

                    <div class="mt-4 text-center">
                        <a asp-action="Index" class="link-secondary text-decoration-underline">← Back to Lecturers List</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Animate.css -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

<style>
    .form-control {
        transition: all 0.3s;
    }

        .form-control:focus {
            border-color: #0d6efd;
            box-shadow: 0 0 8px rgba(13, 110, 253, 0.25);
        }
</style>
